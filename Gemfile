source 'https://rubygems.org'

gem 'jekyll'

group :jekyll_plugins do
	gem 'jekyll-feed'
	gem 'jekyll-seo-tag'
	gem 'jekyll-paginate-v2'
	gem 'jekyll-archives'
	gem 'compass'
	gem 'jekyll_image_encode'
	gem 'jekyll-sitemap'
	# gem 'nokogiri', '1.6.7.2' # this is the version of nokogiri that cloudcannon likes. Note you must turn on experimental builder in cloudcannon.
	#gem 'algoliasearch-jekyll' # depends on nokogiri. Never push this gem installed to production.
end

